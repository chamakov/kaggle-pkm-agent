
## GitHub Repository Syncing
- **ALWAYS** run `git push` to sync the local code with the remote repository after making ANY changes to the codebase, and explicitly notify the user that you have done so. The user often trains remotely (e.g. on Colab) and relies on the remote repository being up-to-date with your latest edits.
