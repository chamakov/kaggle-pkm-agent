## Análisis Individual de Replays y Logs

### Match 83756974
- **Resultado**: Derrota ❌
- **Duración**: 124 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83756974.json` y su log respectivo), el agente llegó hasta el paso 124. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que terminara perdiendo es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83757624
- **Resultado**: Derrota ❌
- **Duración**: 27 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83757624.json` y su log respectivo), el agente llegó hasta el paso 27. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que terminara perdiendo es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83758303
- **Resultado**: Derrota ❌
- **Duración**: 38 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83758303.json` y su log respectivo), el agente llegó hasta el paso 38. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que terminara perdiendo es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83759117
- **Resultado**: Derrota ❌
- **Duración**: 195 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83759117.json` y su log respectivo), el agente llegó hasta el paso 195. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que terminara perdiendo es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83759942
- **Resultado**: Derrota ❌
- **Duración**: 240 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83759942.json` y su log respectivo), el agente llegó hasta el paso 240. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que terminara perdiendo es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83760779
- **Resultado**: Victoria 🏆
- **Duración**: 86 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83760779.json` y su log respectivo), el agente llegó hasta el paso 86. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que lograra ganar es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83761426
- **Resultado**: Victoria 🏆
- **Duración**: 218 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83761426.json` y su log respectivo), el agente llegó hasta el paso 218. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que lograra ganar es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83762213
- **Resultado**: Derrota ❌
- **Duración**: 167 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83762213.json` y su log respectivo), el agente llegó hasta el paso 167. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que terminara perdiendo es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83762895
- **Resultado**: Derrota ❌
- **Duración**: 256 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83762895.json` y su log respectivo), el agente llegó hasta el paso 256. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que terminara perdiendo es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83763714
- **Resultado**: Derrota ❌
- **Duración**: 174 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83763714.json` y su log respectivo), el agente llegó hasta el paso 174. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que terminara perdiendo es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83764529
- **Resultado**: Victoria 🏆
- **Duración**: 174 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83764529.json` y su log respectivo), el agente llegó hasta el paso 174. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que lograra ganar es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83765227
- **Resultado**: Derrota ❌
- **Duración**: 82 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83765227.json` y su log respectivo), el agente llegó hasta el paso 82. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que terminara perdiendo es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83766033
- **Resultado**: Derrota ❌
- **Duración**: 82 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83766033.json` y su log respectivo), el agente llegó hasta el paso 82. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que terminara perdiendo es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83766747
- **Resultado**: Victoria 🏆
- **Duración**: 219 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83766747.json` y su log respectivo), el agente llegó hasta el paso 219. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que lograra ganar es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83767528
- **Resultado**: Victoria 🏆
- **Duración**: 205 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83767528.json` y su log respectivo), el agente llegó hasta el paso 205. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que lograra ganar es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83768171
- **Resultado**: Derrota ❌
- **Duración**: 118 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83768171.json` y su log respectivo), el agente llegó hasta el paso 118. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que terminara perdiendo es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83795580
- **Resultado**: Victoria 🏆
- **Duración**: 261 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83795580.json` y su log respectivo), el agente llegó hasta el paso 261. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que lograra ganar es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83817643
- **Resultado**: Victoria 🏆
- **Duración**: 104 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83817643.json` y su log respectivo), el agente llegó hasta el paso 104. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que lograra ganar es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83834281
- **Resultado**: Derrota ❌
- **Duración**: 396 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83834281.json` y su log respectivo), el agente llegó hasta el paso 396. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que terminara perdiendo es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

### Match 83876989
- **Resultado**: Derrota ❌
- **Duración**: 355 turnos
- **Estado Técnico**: El modelo RL NO se cargó (sb3_contrib no disponible). Jugó aleatoriamente.
- **Análisis de la partida**: En este archivo (`83876989.json` y su log respectivo), el agente llegó hasta el paso 355. Al leer el archivo de logs individual, descubrimos un error fatal: Kaggle no encontró la librería `sb3_contrib`. Esto activó el bloque `except ImportError` en nuestro `main.py`, causando que el agente realizara movimientos **completamente aleatorios** durante toda la partida. El hecho de que terminara perdiendo es resultado de la aleatoriedad y no refleja la inteligencia del modelo entrenado.

